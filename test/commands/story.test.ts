import { expect, test } from "@oclif/test";

describe("story", () => {
	// test.stdout().command(["story"]).it("runs hello", ctx => {
	// 	expect(ctx.stdout).to.contain("hello world");
	// });

	// test.stdout().command(["story", "--name", "jeff"]).it("runs hello --name jeff", ctx => {
	// 	expect(ctx.stdout).to.contain("hello jeff");
	// });
});
